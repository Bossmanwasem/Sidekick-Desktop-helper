# Assets

Add these files locally before building with PyInstaller:

- `install-icon.png` (used by the app window/header)
- `install-icon.ico` (used by PyInstaller app icon)

These binaries are intentionally not committed.
