# Assets

Add these files locally before building the installer:

- `install-icon.png` (used by the app window/header)
- `install-icon.ico` (used by PyInstaller and Inno Setup installer icon)

These binaries are intentionally not committed.
